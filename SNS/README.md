# Script to send text messages using AWS SNS.

Commands used:

```
$ mkdir SNS
$ cd SNS
$ npm init --y
$ touch index.js
$ touch .env
$ npm i @aws-sdk/client-sns
$ npm install --save dotenv
$ node index.js
```